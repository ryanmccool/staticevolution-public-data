{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c 'datasette serve staticevolution.db --host 0.0.0.0 --port ${PORT} --metadata metadata.json --secret ${DATASETTE_SECRET} --immutable staticevolution.db --setting sql_time_limit_ms 15000 --setting allow_download off'",
    "healthcheckPath": "/-/ready",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
